__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Local model config - no secrets in this project, but keep the convention
.env

# Persisted vector store - regenerated by `python -m app.main ingest`
chroma/

# Generated evaluation artefacts (kept in repo once populated by a real run;
# ignore only the scratch state some tools create)
.DS_Store
