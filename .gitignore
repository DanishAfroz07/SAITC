__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# .env is intentionally tracked (see README "Why one .env file") - it holds
# no secrets, only local model/config defaults.

# Persisted vector store - regenerated by `python -m app.main ingest`
chroma/

.DS_Store
