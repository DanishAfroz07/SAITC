# Copy this file to .env before running the app. Nothing here is secret -
# it's all local model/config values - but .env is the conventional place to
# override defaults per machine, so we keep the split anyway.

# The date the assignment asks us to answer "what is current" as of.
# Change this to move the assistant's notion of "today" without touching code.
AS_OF_DATE=2026-08-27

# --- Paths (relative to project root) ---
DOCUMENTS_DIR=data/documents
MANIFEST_PATH=data/corpus_manifest.json
CHROMA_DIR=chroma
COLLECTION_NAME=cerulean_corpus

# --- Ollama (local model server) ---
OLLAMA_HOST=http://localhost:11434
OLLAMA_LLM_MODEL=llama3.2:3b
OLLAMA_EMBED_MODEL=nomic-embed-text

# --- Retrieval / evidence tuning ---
TOP_K=6
SIMILARITY_THRESHOLD=0.35
AMBIGUITY_MARGIN=0.05
CHUNK_MAX_CHARS=1200

# --- API server ---
API_HOST=0.0.0.0
API_PORT=8000
